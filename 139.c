/* Q138: Print all enum names and integer values using a loop.*/

#include<stdio.h>

enum Color { RED, YELLOW, GREEN };

int main() {
    printf("RED = %d\n", RED);
    printf("YELLOW = %d\n", YELLOW);
    printf("GREEN = %d\n", GREEN);
    return 0;
}