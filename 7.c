#include <stdio.h>

    struct student {
        char name[100];
        float marks[3];
        float average;

    };
    int main() {
     struct student s;
     int i, total = 0;

     printf("enter student name: ");
     scanf("%s", s.name);
    printf("enter marks in 5 subjects");
for(i = 0; i<5; i++) {
    printf("subject %d:", i+1);
}   
    }
